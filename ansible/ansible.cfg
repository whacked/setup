[defaults]
# YAML-formatted task output. The old `stdout_callback = yaml` (community.general)
# was removed in community.general 12; the builtin default callback now does it
# via this option (ansible-core >= 2.13).
callback_result_format = yaml
